# Startup course seeding is covered by focused configuration tests. Keep the
# full Spring test suite deterministic and independent of the external OSRM API.
app.official-course.startup-seed.enabled=false
