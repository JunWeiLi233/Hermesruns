Enable the Caveman response skill for this session.

Use `.claude/skills/caveman/SKILL.md`.

Default to `lite` unless the user explicitly asks for `full` or `ultra`.

Apply Caveman to assistant responses and progress updates only. Do not use it for committed UI copy, README prose, or other user-facing product text.
