# Cross-Agent Sync

Updated: 2026-06-22T04:04:41.483-04:00

Use this file as the shared cross-platform coordination layer for Codex, Claude, and other Hermes-capable agents.

## Rules
- Read this file before starting queue work, resuming a checkpoint, or reclaiming a user-visible task.
- Claim a task before implementation when the work unit is not trivially local.
- Do not re-pick recently completed work unless there is a recorded must-fix, regression, or explicit user request.
- Reviewer must-fix items outrank fresh speculative ideas.
- Before self-generated follow-up rounds, also read `.ai-sync/HUMAN_LOOP.md` for human steering, pause, or reversal requests.
- Keep entries short and overwrite stale claims instead of appending long history.

## Active Claims
- Key: territory-auto-hermes-self-fix-territory-filled-land-algorithm-and-codex-native-
  Task: Continue territory filled-land algorithm match to user reference
  Surface: Territory / auto-hermes-self
  Agent: codex
  Owner: codex
  Status: active
  Started: 2026-06-19T21:02:35.5293497-04:00
  Verify: cd backend && ./mvnw -q -Dtest=TerritoryPolygonComputerTests test && ./mvnw -q -DskipTests compile; cd backend && ./mvnw -q "-Dtest=TerritoryControllerTests#" test; node frontend/src/pages/territoryBackendWiring.smoke.test.js; node frontend/src/pages/territoryHeatmapWorldMap.smoke.test.js; node .tools/verify-territory-api-geometry.mjs --url http://localhost:8080; node .tools/verify-territory-polygon-variant-etags.mjs --url http://localhost:8080; node .tools/verify-territory-real-server-markers.mjs --url http://localhost:8080 --asset-pattern "assets/Territory-[A-Za-z0-9_-]+\.js" --marker lower-east-connector --marker lower-east-bridge --marker global-owner-territory-cache-v822-preserve-detached-source-territory --marker support-grid-body --marker routeSupportedFill --forbid-marker lower-east-visible-bridge-junction --forbid-marker FOOTPRINT_BRIDGE_WIDTH --forbid-marker "best.y + 1" --forbid-marker "best.minX - 2" --forbid-marker "terr-land-mask-concrete-land--route-supported-fill" --forbid-marker compactRouteSupportedFillOpacity --require-no-store; cd frontend && node scripts/run-vite-build.mjs; node .tools/verify-frontend-runtime-sync.mjs --files frontend/src/pages/Territory.jsx frontend/src/pages/territoryBackendWiring.smoke.test.js frontend/src/pages/territoryHeatmapWorldMap.smoke.test.js frontend/src/styles/_split/territory.css .tools/verify-territory-filled-land-runtime.mjs; node .tools/verify-backend-runtime-sync.mjs --files backend/src/main/java/com/hermes/backend/TerritoryPolygonComputer.java backend/src/main/java/com/hermes/backend/TerritoryService.java backend/src/test/java/com/hermes/backend/TerritoryPolygonComputerTests.java .tools/verify-territory-api-geometry.mjs; node .tools/verify-territory-filled-land-runtime.mjs --url http://localhost:8080 --coverage-samples 240 --screenshot task-images/territory-filled-land-runtime-proof-current.png --proof-json task-images/territory-filled-land-runtime-proof-current.json; node .tools/verify-territory-reference-score.mjs --screenshot task-images/territory-filled-land-runtime-proof-current.png --proof-json task-images/territory-filled-land-runtime-proof-current.json --score-json task-images/territory-filled-land-runtime-proof-current-score.json
  Files: backend/src/main/java/com/hermes/backend/TerritoryPolygonComputer.java | backend/src/main/java/com/hermes/backend/TerritoryService.java | backend/src/test/java/com/hermes/backend/TerritoryPolygonComputerTests.java | frontend/src/pages/Territory.jsx | frontend/src/pages/territoryBackendWiring.smoke.test.js | frontend/src/pages/territoryHeatmapWorldMap.smoke.test.js | frontend/src/styles/_split/territory.css | .tools/verify-territory-filled-land-runtime.mjs | .tools/score-territory-reference.mjs | .tools/verify-territory-reference-score.mjs | .tools/verify-territory-real-server-markers.mjs | .tools/verify-territory-polygon-variant-etags.mjs | .tools/auto-hermes-finish.mjs | .tools/auto-hermes-finish.test.mjs

## Recently Completed
- 2026-06-22: Continued Territory objective without DeepSeek or browser display/control tooling. Root-caused a frontend rendering risk that could reconnect separate backend regions after ownership union: `repairMergedOwnerMaskTiles(...)` preserved exact cells, but active fill immediately called `repairConsistentMaskTiles(...)`, which could run `bridgeNearbyMaskComponents(...)` again after union. Added a red backend-wiring smoke guard, then changed `repairConsistentMaskTiles(...)` to honor `bridgeComponents: false` and passed that option only in the post-union active-fill repair path. Source-level seam repair remains available before ownership resolution. Verification passed: backend wiring smoke, Heatmap world-map smoke, live API geometry proof, frontend production build, frontend runtime sync, strict HTTP marker proof against served `assets/Territory-CnhNwNL4.js` requiring `bridgeComponents` and forbidding `lower-east-visible-bridge-junction`, and scoped `git diff --check` with only LF/CRLF warnings. Added `DV-2026-06-22-01`. Live browser screenshot/heatmap scorer acceptance remains pending under the browser-proof restriction.
- 2026-06-22: Continued Territory objective without DeepSeek or browser display/control tooling. Hardened the new non-browser live API geometry proof into the frontend regression suite: `territoryBackendWiring.smoke.test.js` now loads `.tools/verify-territory-api-geometry.mjs` and guards the own-footprint audit, pending-backfill rejection, exact area reconciliation, `cellMeters^2` land area check, self-intersection check, spike/slab/broad-route-corridor rejection, and detached-region preservation. Strengthened `.tools/verify-territory-api-geometry.mjs` so detached active land preservation is a hard assertion (`detachedLandRegions > 0`) instead of only a printed metric. Verification passed: verifier syntax, backend wiring smoke, heatmap world-map smoke, live API geometry proof (`detachedLandRegionsPreserved=319`), and scoped `git diff --check` with only LF/CRLF warnings. Live browser screenshot/heatmap scorer acceptance remains pending under the browser-proof restriction.
- 2026-06-22: Continued Territory backend/API geometry fix without DeepSeek or browser display/control tooling. Added `.tools/verify-territory-api-geometry.mjs`, a non-browser authenticated real-server audit that checks `ownFootprint=true` includes active land plus route-corridor masks, no completed-run backfill remains pending, active completed-run cells exceed the default payload, reported total area equals summed polygon area, land area equals cleaned cell count times `cellMeters^2`, coordinate rings do not self-intersect when present, land components are not sparse boxes/spikes, and broad route corridors are rejected. Backend now bumps stored masks to `mask:v38` and response cache to `land-mask-union-v66-spike-clean-land`; `TerritoryPolygonComputer` and `TerritoryService` remove spike-like disconnected land fragments while preserving valid separate regions. Added `TerritoryControllerTests#polygonsEndpointRemovesSpikeLikeLandFragmentsWithoutDroppingSeparateRegions`. Verification passed: verifier syntax, backend compile, `TerritoryPolygonComputerTests`, targeted controller path tests, backend wiring smoke, backend runtime sync, `git diff --check`, backend restart/warmup, and live API geometry proof (`ownFootprint=true`: 556 polygons, 369468 cells, 47 active land masks, 509 route-corridor masks, total area `48505582.384`, detached land regions preserved `319`). Live visual heatmap/screenshot scorer acceptance remains pending under the browser-proof restriction.
- 2026-06-22: Continued Territory work without DeepSeek or browser display/control tooling. Fixed a backend missing-run bug where fully internal completed-run loops in one activity were rejected or demoted because loop qualification was endpoint-only and interior acceptance divided each loop by the whole activity distance. `TerritoryPolygonComputer` now permits locally closed internal loops only when they are far from global route endpoints and not part of a near-closed whole-route miss, removes the whole-activity-distance veto for accepted interior regions, and bumps stored masks to `mask:v37`; `TerritoryService` lightweight decoding now expects `v37` so stale rows recompute. Added `landMaskPreservesDetachedMidRouteLoopsWithoutFillingTransit` regression proving two internal closed loops fill while the transit midpoint stays empty. Also preserved frontend detached source-backed visible clusters and bumped the served cache marker to `global-owner-territory-cache-v822-preserve-detached-source-territory`. Verification passed: full `TerritoryPolygonComputerTests`, targeted `TerritoryControllerTests`, backend compile, backend wiring smoke, heatmap smoke, frontend build/runtime sync, backend runtime sync, strict HTTP marker proof against served `assets/Territory-CiTERDZo.js`, and live polygon variant verifier after backend restart/warmup (`ownFootprint=true`: 563 polygons, 394535 cells, 509 route-corridor polygons, 54 land polygons). Live screenshot/scorer acceptance remains pending under browser-proof restriction.
- 2026-06-22: Hardened the non-browser Territory real-server API proof. `.tools/verify-territory-polygon-variant-etags.mjs` now asserts the live `ownFootprint=true` payload is route-corridor/cell-heavy, not only variant-fresh: it must include active route-corridor polygons, active land polygons, and more concrete cells than the default initial payload. Current live proof passed with `ownFootprint=true` returning 635 polygons, 673043 cells, and 617 route-corridor polygons. Backend wiring smoke and verifier syntax passed. Live screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued Territory source coverage work without DeepSeek or browser display/control tooling. Removed the remaining local route-supported starvation caps in `activeRouteSupportedCellFillRegionGroups(...)`: filled route-supported records now use `LAND_MASK_ACTIVE_ROUTE_CORRIDOR_LAND_MAX_GROUPS` instead of an extra 8-record cap, and sparse support can reserve up to 6 records instead of 4. Existing density/aspect/span and transparent raw `route-supported` gates remain. Verification passed: backend wiring smoke, heatmap smoke, filled-land verifier syntax, frontend build, frontend runtime sync, live polygon variant verifier, strict HTTP marker proof against served `assets/Territory-CSyU2RlF.js`, and `git diff --check` with only LF/CRLF warnings. Live screenshot/scorer acceptance remains pending.
- 2026-06-22: Hardened the Territory filled-land runtime verifier so any visible raw `route-supported` active layer now fails proof; real coverage must be promoted through `supported-body` layers instead. This locks the rejected opacity-only workaround out of the future screenshot/scorer helper. Verification passed: backend wiring smoke, heatmap smoke, filled-land verifier syntax. Live screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued Territory work without DeepSeek or browser display/control tooling. Rejected the prior `compactRouteSupportedFillOpacity(...)` direction because it made raw `route-supported` evidence visible and conflicted with the proof model. Raw route support is transparent again; `activeSupportedTerritoryBodyRegionGroups(...)` now iteratively pulls connected, bounded `route-supported` clusters into supported-body coverage using area/span/aspect/fill-density gates. Verification passed: backend wiring smoke, heatmap smoke, live polygon variant verifier, frontend build, frontend runtime sync, strict HTTP marker proof against served `assets/Territory-Dek34ajN.js` requiring supported-body markers and forbidding `route-supported-fill`/`compactRouteSupportedFillOpacity`, and `git diff --check` with only LF/CRLF warnings. Live screenshot/scorer acceptance remains pending.

## Must-Fix Queue
- Key: territory-auto-hermes-self-fix-territory-filled-land-algorithm-and-codex-native-
  Task: Fix territory filled-land algorithm and Codex-native auto-hermes-self delegation
  Surface: Territory / auto-hermes-self
  Agent: codex
  Owner: codex
  Status: must-fix
  Started: 2026-06-19T04:49:43.529Z
  Verify: node frontend/src/pages/territoryBackendWiring.smoke.test.js; node frontend/src/pages/territoryHeatmapWorldMap.smoke.test.js; cd frontend && node scripts/run-vite-build.mjs; node .tools/verify-frontend-runtime-sync.mjs --files frontend/src/pages/Territory.jsx frontend/src/pages/territoryBackendWiring.smoke.test.js .tools/verify-territory-filled-land-runtime.mjs; node .tools/verify-territory-filled-land-runtime.mjs --url http://localhost:8080 --coverage-samples 240 --screenshot task-images/territory-filled-land-runtime-proof.jpg
  Files: frontend/src/pages/Territory.jsx | frontend/src/pages/territoryBackendWiring.smoke.test.js | frontend/src/styles/_split/territory.css | .tools/verify-territory-filled-land-runtime.mjs | .codex/commands/auto-hermes-self.md
  Review: ralph-gate-must-fix

- Key: profile-fix-profile-empty-state
  Task: Fix Profile empty state
  Surface: Profile
  Agent: codex
  Status: must-fix
  Started: 2026-06-17T06:53:44.434Z
  Verify: `cd frontend && npm run lint && npm run build`
  Files: frontend/src/pages/Profile.jsx
  Review: ralph-gate-must-fix

## Human Inbox
- none

## Latest Notes
- 2026-06-22: Fixed a remaining frontend-side cluster-connection risk. Active fill now calls `repairConsistentMaskTiles(repairedTiles, renderGrid, { largeLandmass: activeLargeLandmass, bridgeComponents: false })` after owner union, preventing renderer-side component bridges from reconnecting separate backend regions. Served `assets/Territory-CnhNwNL4.js` contains the new marker. Verification passed: backend wiring smoke, Heatmap world-map smoke, production build, runtime sync, HTTP marker proof, and live API geometry proof. Browser screenshot/scorer acceptance remains unrun due restriction.
- 2026-06-22: Tightened non-browser Territory regression coverage. The backend wiring smoke test now guards `.tools/verify-territory-api-geometry.mjs`, and the verifier now asserts `detachedLandRegions > 0`, so preserving separate completed-run land regions is enforced. Passed syntax, backend wiring smoke, heatmap world-map smoke, and live API geometry proof (`556` polygons, `369468` active cells, `319` detached land regions preserved). Browser screenshot/scorer acceptance remains unrun due restriction.
- 2026-06-22: Continued the user's "current territory not covered correct / still messy" backend side without DeepSeek or browser display/control tooling. Live API geometry now passes after v38/v66 cleanup: `ownFootprint=true` returns 556 polygons, 369468 cells, 47 active land masks, 509 route-corridor masks, exact total-area reconciliation `48505582.384`, and 319 detached land regions preserved. The new verifier rejects pending backfill, missing completed-run payloads, self-intersections, land boxes/slabs/spikes, and broad route-corridor fills. Browser screenshot/heatmap scorer acceptance was not run because the user restricted browser display/control checks.
- 2026-06-22: Continued the active Territory goal without DeepSeek or browser display/control tooling. Strengthened `.tools/verify-territory-polygon-variant-etags.mjs` so direct real-server API proof now verifies the `ownFootprint=true` response contains the route-corridor/cell-heavy coverage required for full Territory rendering. It counts `routeCorridorPolygons` and `landPolygons`, and asserts own-footprint cells exceed the default payload. Current proof: default=56 polygons/256386 cells/39 route-corridor polygons; ownFootprint=true=635 polygons/673043 cells/617 route-corridor polygons. Verification passed: backend wiring smoke, live polygon variant verifier, verifier syntax. Live visual/scorer acceptance remains pending.
- 2026-06-22: Continued the user's incomplete/messy Territory coverage report without DeepSeek or browser display/control tooling. Backend API proof shows the active owner has many route-corridor polygons/cells, while source inspection found `activeRouteSupportedCellFillRegionGroups(...)` still capped eligible filled support at 8 and sparse support at 4 before the configured 22-group route-supported budget could apply. Added a red smoke guard for this cap, then changed the cap to use `LAND_MASK_ACTIVE_ROUTE_CORRIDOR_LAND_MAX_GROUPS` and allow up to 6 sparse records. This moves more qualifying supported clusters into the existing guarded polygonization path without making raw `route-supported` visible. Verification passed: backend wiring smoke, heatmap smoke, filled-land verifier syntax, frontend build, frontend runtime sync, live polygon variant verifier, strict HTTP marker proof against `assets/Territory-CSyU2RlF.js`, and `git diff --check` with only LF/CRLF warnings. Live visual/scorer acceptance remains pending.
- 2026-06-22: Continued Territory verifier hardening without DeepSeek or browser display/control tooling. `verify-territory-filled-land-runtime.mjs` now rejects every filled raw `route-supported` layer (`fillOpacity > 0.02`) instead of allowing compact/dense route-supported fill. Backend wiring smoke guards the contract so future coverage fixes must promote real clusters through `supported-body`, not CSS/opacity-visible route evidence. Verification passed: backend wiring smoke, heatmap smoke, filled-land verifier syntax. Live authenticated screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued Territory work without DeepSeek or browser display/control tooling. Direct API checks showed the running real server was stale: `/api/territory/polygons`, `?cells=false`, `?initial=true`, and `?ownFootprint=true` returned different payload shapes but the same ETag/signature, despite source and tests already containing variant-aware signatures. Targeted backend tests passed for `polygonsEndpointDoesNotCacheCellsFalseAsCanonicalPayload` and `polygonsEndpointReturnsNotModifiedWhenClientCacheSignatureMatches`; backend compile passed. Restarted only the stale Spring Boot listener on port 8080 through `.tools/run-backend.cmd` hidden. Live API proof now shows distinct variant signatures/ETags for distinct payload variants, cross-variant `If-None-Match` returns `200`, and same-variant `If-None-Match` returns `304`. Also passed backend runtime sync for `TerritoryController.java`, backend wiring smoke, heatmap smoke, and strict HTTP marker proof against served `assets/Territory-CKhwb8_Z.js`. Live visual screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued Territory work without DeepSeek or browser display/control tooling. Fixed an incomplete-coverage gate in `activeRouteSupportedCellFillRegionGroups`: sparse backed route-support records were selected but then sent through `sparseActiveRouteCellFootprintRegionGroup(...)`, which was hard-disabled. The sparse fallback now polygonizes bounded connected source tiles directly (`maskTileConnectedComponents` plus `maskBoundaryLoops`) with min-tile, detached-area, span, and aspect gates, so qualifying sparse supported clusters can render without broad bounds rectangles. Smoke guards now reject both hard-disabling this path and `return [[...]]` rectangle restoration. Verification passed: backend wiring smoke, heatmap smoke, frontend build, frontend runtime sync, strict HTTP marker proof against served `assets/Territory-CKhwb8_Z.js`, and `git diff --check` with only LF/CRLF warnings. Live screenshot/scorer acceptance was not rerun because browser display/proof tooling remains restricted.
- 2026-06-22: Continued the Territory goal without DeepSeek or browser display/control tooling. Added `--forbid-marker` support to `.tools/verify-territory-real-server-markers.mjs` so the HTTP-served Territory chunk proof now verifies both required markers and rejected-marker absence through the same asset-pattern-filtered chunk graph. Wired the active claim/checkpoint proof to reject `lower-east-visible-bridge-junction`, `FOOTPRINT_BRIDGE_WIDTH`, `best.y + 1`, and `best.minX - 2` while still requiring `lower-east-connector`, `lower-east-bridge`, and the cache marker. Verification passed: verifier syntax, backend wiring smoke, heatmap smoke, intentional negative forbidden-marker check, strict HTTP proof against `assets/Territory-B2qkyp3p.js`, and `git diff --check` with only LF/CRLF warnings. Live authenticated screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued the Territory goal without DeepSeek or browser display/control tooling. Tightened the active claim and checkpoint required HTTP proof so future non-browser real-server marker verification must require `lower-east-connector` in addition to `lower-east-bridge` and `global-owner-territory-cache-v821-payload-aware-territory`. This prevents the restored bounded lower-east connector from silently disappearing in later source/static checks. Verification passed: backend wiring smoke, heatmap smoke, strict HTTP marker proof against `assets/Territory-B2qkyp3p.js`, and `git diff --check` with only LF/CRLF warnings. Live authenticated screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued the Territory goal without DeepSeek or browser display/control tooling. Restored the lower-east connector as a bounded source-derived narrow corridor instead of leaving `activeRouteSupportedLowerEastConnectorRegions(...)` hard-disabled. The connector now emits only when lower-east bridge and trimmed evidence bounds are east-west adjacent, gap is 15-420m, vertical miss is <=380m, height is capped to 56m, and output is capped to two connector regions; it does not restore the rejected broad `return [[...]]` strip or `lower-east-visible-bridge-junction` rectangle recovery. Smoke guards now require these caps and reject hard-disabling the connector again. Verification passed: backend wiring smoke, heatmap smoke, scorer/reference-score/marker syntax, frontend build, frontend runtime sync with edited Territory files, strict HTTP real-server marker proof against `assets/Territory-B2qkyp3p.js` containing `lower-east-connector`, `lower-east-bridge`, and the cache marker, served chunk absence check for rejected junction/fixed-tail/footprint strings, and `git diff --check` with only LF/CRLF warnings. Live authenticated screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued the Territory goal without DeepSeek or browser display/control tooling. Aligned `activeRouteTraceMetrics(...)` with the 6-point route-trace rescue floor by changing its early rejection from `LAND_MASK_ACTIVE_ROUTE_SKELETON_MIN_POINTS` (12) to `LAND_MASK_ACTIVE_ROUTE_TRACE_CHUNK_MIN_POINTS` (6). This removes a second stale 12-point gate that could still drop sparse backed route traces before metric-derived narrow corridor fill, while preserving the route length/span/chunk/density safeguards. Smoke guards now require the metric helper to use the same 6-point floor and reject restoring the old 12-point metric floor. Verification passed: backend wiring smoke, heatmap smoke, scorer/reference-score/marker syntax, frontend build, frontend runtime sync with edited Territory files, strict HTTP real-server marker proof against `assets/Territory-9oIf_nDZ.js`, served chunk absence check for rejected junction/fixed-tail/connector/footprint strings, and source absence search for stale 12-point trace filters. Live authenticated screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued the Territory goal without DeepSeek or browser display/control tooling. Relaxed the narrow route-trace rescue eligibility from the old 12-point skeleton floor to the existing 6-point chunk floor in both `activeRouteSkeletonTraceLatLngs(...)` and `activeRouteTraceCorridorRegionGroups(...)`, while preserving the 650m length gate, 8500m span gate, chunking, minimum corridor-tile count, dense-block rejection, and compact-rectangle rejection. This lets valid sparse 6-11 point backed route traces reach the evidence-derived narrow corridor rasterizer instead of being dropped before evaluation. Smoke guards now require the 6-point path and reject restoring the 12-point prefilter. Verification passed: backend wiring smoke, heatmap smoke, scorer/reference-score/marker syntax, old-pattern absence search, frontend build, frontend runtime sync, strict HTTP real-server marker proof against `assets/Territory-9oIf_nDZ.js`, served chunk absence check for rejected junction/fixed-tail/connector/footprint strings, and `git diff --check` with only LF/CRLF warnings. Live authenticated screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued the Territory goal without DeepSeek or browser display/control tooling. Removed two more rectangle/slab sources from the active code path: `activeRouteSupportedLowerEastConnectorRegions(...)` now returns `[]` instead of a bounds-derived connector strip, and dormant `detachedActiveSatelliteRegionGroup(...)` now returns `null` instead of inset bounding-box fragments. Smoke guards now require both to stay disabled and reject `return [[...]]` restoration. Verification passed: backend wiring smoke, heatmap smoke, verifier syntax checks, source forbidden-pattern search, frontend build, frontend runtime sync, strict HTTP real-server marker proof against `assets/Territory-BxiAizk2.js`, served chunk absence check for rejected junction/fixed-tail/footprint-bridge/connector strings, and `git diff --check` with only LF/CRLF warnings. Live authenticated screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued the Territory goal without DeepSeek or browser display/control tooling. Regression-hardening pass found dormant synthetic rectangle helpers: `activeRouteSupportedFootprintBridgeRegion(...)` could generate bounding-box bridge polygons and `activeRouteTraceBoundsRegionGroup(...)` could generate padded route-trace bounding boxes if reconnected. Both now return `null`, the unused footprint-bridge constants were removed, and smoke guards require these fallback rectangle paths to stay disabled. Verification passed: backend wiring smoke, heatmap smoke, scorer/reference-score/marker syntax, frontend build, frontend runtime sync, strict HTTP real-server marker proof against `assets/Territory-DGj8Mdbk.js`, served chunk absence check for rejected junction/fixed-tail/footprint-bridge strings, and `git diff --check` with only LF/CRLF warnings. This reduces future slab regression surface; live authenticated screenshot/scorer acceptance remains pending.
- 2026-06-22: Continued the Territory goal without DeepSeek or browser display/control tooling. Root-cause follow-up: after removing the rejected junction recovery, `activeRouteSupportedLowerEastBridgeRegions(...)` still emitted a hard-coded southward bridge tail (`best.y + 1..+5`, `best.minX - 2..best.minX`) that could create stacked slabs. Replaced it with an evidence-bounded bridge that only paints rows inside the actual support/evidence latitude overlap, capped around the overlap center, and added smoke guards preventing the old fixed tail from returning. Verification passed: backend wiring smoke, heatmap smoke, scorer/wrapper/marker syntax, frontend build, frontend runtime sync, strict HTTP real-server marker proof against `assets/Territory-DGj8Mdbk.js`, served chunk absence check for rejected junction/fixed-tail strings, and `git diff --check` with only LF/CRLF warnings. Live authenticated screenshot/scorer acceptance remains pending.
- 2026-06-22: User reported current Territory display is still messy and incomplete. No DeepSeek or browser display/control tooling was used; inspected the stored proof image. Removed the unaccepted `lower-east-visible-bridge-junction` rectangle recovery from `Territory.jsx`, updated smoke/scorer guidance so the rejected rectangle path stays disabled, rebuilt/synced frontend, and verified the real server now serves `assets/Territory--c4ZFu6V.js` without the rejected marker. Also added proof-origin binding so scorer/wrapper proof JSON defaults to `http://localhost:8080`. Verification passed: scorer/wrapper syntax, backend wiring smoke, heatmap smoke, frontend build, runtime sync, HTTP marker proof, rejected-marker absence check, wrong-origin proof rejection, and `git diff --check` with only LF/CRLF warnings. Broader visual coverage/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Hardened `.tools/verify-territory-filled-land-runtime.mjs` so an existing `--proof-json` artifact is removed at startup before proof work runs, preventing stale proof JSON from surviving a failed future visual verifier run. `territoryBackendWiring.smoke.test.js` guards stale proof cleanup plus atomic proof persistence. Verification passed: verifier syntax, backend wiring smoke, heatmap smoke, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, and `git diff --check` on touched files with only LF/CRLF warning. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Added `.tools/verify-territory-filled-land-runtime.mjs` to `.tools/auto-hermes-finish.mjs` publishable tooling classification and updated `.tools/auto-hermes-finish.test.mjs` so the filled-land proof helper is auto-included with the Territory marker/scorer wrappers. Verification passed: finish helper syntax, auto-hermes finish test, backend wiring smoke, heatmap smoke, filled-land verifier syntax, and strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Hardened `.tools/verify-territory-filled-land-runtime.mjs` proof JSON persistence so `--proof-json` writes to a process-scoped temp file and atomically renames into place after full serialization succeeds; temp output is removed on failure. `territoryBackendWiring.smoke.test.js` guards the atomic proof write. Verification passed: verifier/scorer syntax, backend wiring smoke, heatmap smoke, auto-hermes finish test, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, and a positive temporary proofUrl wrapper score check. Normal current wrapper execution is still blocked by absent `task-images/territory-filled-land-runtime-proof-current.json`; visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Strengthened proof route binding: `.tools/verify-territory-filled-land-runtime.mjs` now writes `proofUrl: proof.url`, and `.tools/score-territory-reference.mjs` requires explicit proof JSON to include an absolute proof URL whose pathname is `/territory` before scoring. `territoryBackendWiring.smoke.test.js` guards both sides. Verification passed: scorer/verifier syntax, backend wiring smoke, heatmap smoke, auto-hermes finish test, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, negative missing/wrong proofUrl scorer checks, positive `/territory?filledLandProof=...` scorer check, and positive temporary proofUrl wrapper score check. Normal current wrapper execution is still blocked by absent `task-images/territory-filled-land-runtime-proof-current.json`; visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Moved `.tools/score-territory-reference.mjs` explicit proof-artifact validation before PNG component scoring, so missing/stale proof fails before reference/screenshot image analysis. `territoryBackendWiring.smoke.test.js` guards proof-before-PNG ordering. Verification passed: scorer syntax, backend wiring smoke, heatmap smoke, auto-hermes finish test, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, a negative missing-proof check with an intentionally bad reference path proving proof validation runs first, and a positive temporary hash/path-bound wrapper score check. Normal current wrapper execution is still blocked by absent `task-images/territory-filled-land-runtime-proof-current.json`; visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Tightened `.tools/score-territory-reference.mjs` so explicit `--proof-json` must include `screenshotPath` from filled-land verifier output before reference scoring uses diagnostics, in addition to the existing ok/hash/rendered-layer/proof-summary checks. `territoryBackendWiring.smoke.test.js` guards the required screenshotPath branch. Verification passed: scorer syntax, backend wiring smoke, heatmap smoke, auto-hermes finish test, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, a negative pathless-proof rejection check, and a positive temporary hash/path-bound wrapper score check. Normal current wrapper execution is blocked by absent `task-images/territory-filled-land-runtime-proof-current.json`; visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Hardened `.tools/verify-territory-reference-score.mjs` so `--score-json` cannot resolve to the screenshot, proof JSON, or reference input before stale-output cleanup runs, preventing accidental deletion of proof inputs by a mistyped score output path. `territoryBackendWiring.smoke.test.js` guards this ordering. Verification passed: wrapper syntax, backend wiring smoke, heatmap smoke, auto-hermes finish test, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, a negative score-json collision check that preserved the screenshot hash, a positive accepted atomic score-json smoke, and a negative stale-output cleanup check. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Tightened `.tools/verify-territory-reference-score.mjs` so optional `--score-json` is persisted only when parsed scorer output includes `acceptance.ok === true`, making the durable score artifact an explicitly accepted threshold result. `territoryBackendWiring.smoke.test.js` guards accepted-score persistence. Verification passed: wrapper syntax, backend wiring smoke, heatmap smoke, auto-hermes finish test, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, a positive accepted atomic score-json smoke with hash-bound proof JSON, and a negative stale-output cleanup check. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Tightened `.tools/verify-territory-reference-score.mjs` so successful scorer stdout must parse to a JSON object, not `null`, an array, or another JSON primitive, before optional `--score-json` persistence. `territoryBackendWiring.smoke.test.js` guards the object-shaped score contract. Verification passed: wrapper syntax, backend wiring smoke, heatmap smoke, auto-hermes finish test, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, a positive atomic score-json smoke with hash-bound proof JSON, and a negative stale-output cleanup check. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Hardened `.tools/verify-territory-reference-score.mjs` so successful `--score-json` output is staged to a process-scoped temporary file and atomically renamed into place after scorer JSON parsing succeeds; stale final output is still removed before scorer execution, and temporary output is cleaned up on write/rename failure. `territoryBackendWiring.smoke.test.js` guards the atomic write contract. Verification passed: wrapper syntax, backend wiring smoke, heatmap smoke, auto-hermes finish test, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, a positive atomic score-json smoke with hash-bound proof JSON, and a negative stale-output cleanup check. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Hardened `.tools/verify-territory-reference-score.mjs` so `--score-json` first removes the requested output path, then only writes a new score artifact after the scorer succeeds and stdout parses as JSON. This prevents stale accepted score JSON from surviving a failed proof/scorer rerun. `territoryBackendWiring.smoke.test.js` guards the stale cleanup. Verification passed: wrapper syntax, backend wiring smoke, heatmap smoke, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, auto-hermes finish test, `git diff --check`, and a negative temp-file check that forced missing-proof failure and confirmed the stale score JSON was removed. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Added `--score-json` to `.tools/verify-territory-reference-score.mjs` so accepted live screenshot scoring can persist a validated scorer JSON artifact while preserving stdout/stderr forwarding. Required verification now writes `task-images/territory-filled-land-runtime-proof-current-score.json` after the filled-land proof JSON exists. `territoryBackendWiring.smoke.test.js` guards the wrapper behavior. Verification passed: wrapper syntax, backend wiring smoke, heatmap smoke, auto-hermes finish test, strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`, `git diff --check` on touched files, and an isolated non-browser `--score-json` smoke using the existing screenshot as self-reference plus a temporary hash-bound proof JSON. Real current proof JSON is absent; visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI was used. Verified `$browser-use-browser` through the actual Browser Use plugin runtime rather than generic browser control: plugin files existed, Node REPL `js` was callable, Chrome extension backend was selected, disposable tab `729435434` opened, navigated to `https://example.com/`, and was closed. No browser display proof was used for `/territory`. Non-browser Territory verification passed: backend wiring smoke, heatmap smoke, auto-hermes finish test, scorer/reference-score/filled-land/real-server marker syntax checks, and strict HTTP marker proof against `assets/Territory-DzrKmJ5J.js`. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. `.tools/verify-territory-reference-score.mjs` now emits `--diagnose-grid` by default when running the reference scorer, with `--no-diagnose-grid` as the opt-out. `territoryBackendWiring.smoke.test.js` guards the default diagnostic behavior. Verification passed: wrapper syntax, backend wiring smoke, heatmap smoke, and source check for the wrapper diagnostic default. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Tightened `.tools/score-territory-reference.mjs` so explicit proof JSON must include positive filled-land verifier summaries (`proof.activeLandPathCount` and `coverage.coverageRatio`) before reference scoring can use it. `territoryBackendWiring.smoke.test.js` now guards this proof-summary requirement. Verification passed: scorer syntax, wrapper syntax, backend wiring smoke, heatmap smoke, and a negative proof-summary rejection check using the current screenshot artifact. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Added `.tools/verify-territory-reference-score.mjs`, a non-browser wrapper around the reference scorer with accepted-baseline defaults (`minIou=0.6078006500541712`, `maxCurrentOnly=2415`, `maxReferenceOnly=481`, `minAllCurrentIou=0.27441036294757426`, `maxAllCurrentCurrentOnly=11397`). The active verification sequence now runs this wrapper after filled-land proof JSON is generated. Finish hygiene marks the wrapper publishable and `territoryBackendWiring.smoke.test.js` guards the thresholds and scorer invocation. Verification passed: wrapper/scorer syntax, backend wiring smoke, heatmap world-map smoke, auto-hermes finish test, strict HTTP real-server marker proof against `assets/Territory-DzrKmJ5J.js`, and `git diff --check` on touched files with only LF/CRLF warnings. Wrapper execution was skipped because current proof JSON is not present; visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Added strict optional acceptance gates to `.tools/score-territory-reference.mjs`: `--min-iou`, `--max-current-only`, `--max-reference-only`, `--min-all-current-iou`, and `--max-all-current-current-only`. When supplied, the scorer now emits an `acceptance` block and exits 1 if any metric fails, so future live screenshot scoring cannot be accidentally treated as acceptable when IoU or mismatch metrics regress. `territoryBackendWiring.smoke.test.js` guards the threshold contract. Verification passed: scorer syntax, backend wiring smoke, heatmap world-map smoke, strict HTTP real-server marker proof against `assets/Territory-DzrKmJ5J.js`, negative threshold check with `--min-iou 0.999`, and `git diff --check` on touched files with only LF/CRLF warning. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Tightened filled-land proof artifact binding: `.tools/verify-territory-filled-land-runtime.mjs` now captures the proof screenshot once as PNG, uses that exact buffer for screenshot analysis, and writes `screenshotSha256` into optional `--proof-json`. `.tools/score-territory-reference.mjs` now requires that hash and rejects proof JSON if the hash does not match the `--screenshot` bytes. `territoryBackendWiring.smoke.test.js` guards both sides of the contract. Verification passed: scorer/verifier syntax, backend wiring smoke, heatmap world-map smoke, strict HTTP real-server marker proof against `assets/Territory-DzrKmJ5J.js`, a negative bad-hash scorer check, and `git diff --check` on touched files with only LF/CRLF warnings. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Confirmed the stricter `.tools/score-territory-reference.mjs` patch is present and extended `territoryBackendWiring.smoke.test.js` so scorer proof JSON must exist, come from an `ok=true` verifier run, match `--screenshot`, and include rendered layer diagnostics before layer-aware scoring can proceed. Verification passed: scorer syntax, backend wiring smoke, heatmap world-map smoke, verifier syntax, strict HTTP real-server marker proof against `assets/Territory-DzrKmJ5J.js`, and `git diff --check` on touched files with only the existing LF/CRLF warning. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Hardened `.tools/score-territory-reference.mjs`: when `--proof-json` is supplied it must exist, and if it records `screenshotPath`, that path must resolve to the same file as `--screenshot`. This prevents mixing a fresh screenshot with stale rendered-layer diagnostics. `territoryBackendWiring.smoke.test.js` now guards the required proof JSON load and proof/screenshot mismatch rejection. Verification passed: scorer syntax, backend wiring smoke, heatmap smoke, strict HTTP marker proof, negative missing-proof-json scorer check, and `git diff --check` with only LF/CRLF warnings. Visual screenshot/scorer acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Added `--proof-json` to `.tools/verify-territory-filled-land-runtime.mjs`, writing the same structured `proofOutput` diagnostics JSON that is printed to stdout. This makes future allowed live visual proof directly scorer-ready via `.tools/score-territory-reference.mjs --proof-json ...`. `territoryBackendWiring.smoke.test.js` now guards the `--proof-json` argument, persisted `proofOutput`, and console output path. Verification passed without launching browser proof: verifier syntax, backend wiring smoke, heatmap smoke, strict HTTP marker proof, and `git diff --check` with only LF/CRLF warnings. Required visual proof command now includes `--proof-json task-images/territory-filled-land-runtime-proof-current.json`.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Found and fixed a real missing-run bug in My Territory: live API/API-list comparison showed `642` GPS-qualified completed runs but only `638` were represented; missing IDs were `559`, `478`, `359`, and `88`. Direct PostgreSQL inspection showed each had thousands/valid GPS points but only processed-empty `mask:v39:land:8.0|` rows. Root cause: `detectTerritoryMasks` used strict land-boundary gates for all output, so short-but-qualified routes, routes with a single large GPS gap, and closure candidates with no valid enclosed land could return empty instead of route corridors. Fixed `TerritoryPolygonComputer` so strict sampling still gates land polygonization, but GPS-qualified routes fall back to route-corridor masks painted only from real short segments, skipping distant gaps; sparse synthetic closed outlines still stay empty. Bumped mask prefix to `mask:v40` and polygon cache to `land-mask-union-v68-route-gap-corridors` so stale processed-empty rows recompute. Hardened `.tools/verify-territory-api-geometry.mjs` to compare `/api/activities` plus `/api/activities/route-previews` against active territory activity coverage. Verification passed: new failing-then-passing unit regressions for short GPS routes and distant gap corridors, full `TerritoryPolygonComputerTests`, spike cleanup controller regression, backend compile, backend wiring smoke, backend/frontend runtime sync, and live API geometry audit after v40 backfill settled. Live result: `polygons=560`, `activeLand=47`, `activeRouteCorridors=513`, `completedRunCoverage runs=681 gpsQualified=642 covered=642`, `active cells=365173`, `land cells=95570`, `routeCorridor cells=269603`, `area total=47364782.384`, `detachedLandRegionsPreserved=272`. The four previously missing rows now store nonzero `mask:v40:route-corridor` masks. Visual heatmap/screenshot acceptance remains not run under current no-browser restriction.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Tightened My Territory completed-run geometry cleanup after the live own-footprint API verifier found residual detached spike tails in active land union `index-148`. Bumped backend mask encoding to `mask:v39` and polygon cache to `land-mask-union-v67-tail-spike-clean-land`; spike cleanup now removes both small <=3-cell ultra-thin fragments and dense 5-6 cell-wide detached tails while preserving wider separate regions. Hardened `.tools/verify-territory-api-geometry.mjs` to inspect every active land component for sparse/spike/slab fragments instead of only the largest component, and extended `TerritoryControllerTests#polygonsEndpointRemovesSpikeLikeLandFragmentsWithoutDroppingSeparateRegions` with 4-cell-wide and dense-tail regressions. Verification passed: targeted controller regression, backend compile, backend wiring smoke, heatmap world-map smoke, frontend production build, frontend runtime sync, backend runtime sync, HTTP-only real-server marker proof, and live API geometry audit against `http://localhost:8080` after backfill settled (`polygons=556`, `activeLand=47`, `activeRouteCorridors=509`, `active cells=364998`, `land cells=95570`, `routeCorridor cells=269428`, `default cells=87204`, `area total=47361262.384`, `detachedLandRegionsPreserved=272`). Visual heatmap/screenshot acceptance remains not run under the current no-browser restriction.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Revalidated the current non-browser Territory proof chain: backend wiring smoke, heatmap world-map smoke, verifier syntax, frontend production build, `verify-frontend-runtime-sync`, and strict HTTP marker proof all passed. The real server still serves `assets/Territory-DzrKmJ5J.js` with `lower-east-visible-bridge-junction` and `global-owner-territory-cache-v821-payload-aware-territory`. Tightened `.tools/verify-territory-real-server-markers.mjs` so `--asset-pattern` fails explicitly when no served asset matches and reports matched assets on PASS; guarded in `territoryBackendWiring.smoke.test.js`. Negative verification with a fake Territory chunk pattern failed as intended. This remains served-static/non-browser proof only; visual screenshot/scorer proof is still pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Tightened `.tools/verify-territory-real-server-markers.mjs` with `--asset-pattern`, allowing marker proof to be restricted to the served Territory chunk instead of any JS asset in the chunk graph. Updated `territoryBackendWiring.smoke.test.js` to guard the asset-pattern filter. Verification passed: verifier syntax check, backend wiring smoke, strict HTTP marker proof with `--asset-pattern "assets/Territory-[A-Za-z0-9_-]+\.js"`, and `git diff --check` on the touched files with only the existing LF/CRLF warning. The real server fetched `60` JS assets and found both required markers specifically in `assets/Territory-DzrKmJ5J.js`. This strengthens served-static freshness proof only; visual screenshot/scorer proof remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Extended the finish-helper publishable path to cover both shared Territory proof tools: `.tools/verify-territory-real-server-markers.mjs` and `.tools/score-territory-reference.mjs`. Updated `.tools/auto-hermes-finish.test.mjs` so `git status` auto-inclusion verifies both files are classified as `publishable`. Verification passed: `node .tools/auto-hermes-finish.test.mjs` (exits 0 with pre-existing benign git stderr), syntax checks for finish/test/scorer/verifier, backend wiring smoke, and the HTTP-only real-server marker verifier. This preserves the proof/scorer artifacts as shared code for eventual publish; it does not change rendered Territory geometry or satisfy visual acceptance.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Re-ran the current non-browser Territory proof chain after the verifier/finish-helper changes. Passed: backend wiring smoke, heatmap world-map smoke, `node .tools/auto-hermes-finish.test.mjs` (exits 0 with pre-existing benign git stderr), frontend production build, `verify-frontend-runtime-sync` with bundle markers `lower-east-visible-bridge-junction` and `global-owner-territory-cache-v821-payload-aware-territory`, the HTTP-only real-server marker verifier, and direct static/live marker checks. Freshly built and synced backend static/live assets still serve `Territory-DzrKmJ5J.js` with both markers in `backend/src/main/resources/static` and `backend/target/classes/static`. Visual screenshot/scorer proof remains not run, so the goal is still active.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Integrated the new HTTP-only Territory real-server marker verifier into the Hermes finish path by marking `.tools/verify-territory-real-server-markers.mjs` as publishable in `.tools/auto-hermes-finish.mjs` and adding a regression in `.tools/auto-hermes-finish.test.mjs` for auto-including it from `git status`. Verification passed: `node .tools/auto-hermes-finish.test.mjs` (prints two pre-existing benign git `fatal: Needed a single revision` lines but exits 0), syntax checks for finish/test/verifier, backend wiring smoke, heatmap smoke, scorer syntax, and the real-server marker verifier against `http://localhost:8080`. This changes finish hygiene only; visual Territory acceptance remains pending.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Added `.tools/verify-territory-real-server-markers.mjs`, an HTTP-only real-server verifier that fetches `/territory`, recursively crawls served JS assets, optionally enforces `no-store` cache headers, and fails unless required markers are present in the served chunk graph. Guarded the verifier in `territoryBackendWiring.smoke.test.js`. Verification passed: `node --check .tools/verify-territory-real-server-markers.mjs`, backend wiring smoke, heatmap world-map smoke, `node --check .tools/score-territory-reference.mjs`, and `node .tools/verify-territory-real-server-markers.mjs --url http://localhost:8080 --marker lower-east-visible-bridge-junction --marker global-owner-territory-cache-v821-payload-aware-territory --require-no-store`. The real server fetched `60` JS assets and found both markers in `assets/Territory-DzrKmJ5J.js`. This proves served static marker freshness only; it is not live visual acceptance.
- 2026-06-22: User explicitly stopped DeepSeek usage; no DeepSeek skill/CLI, browser display/control tooling, or browser tab was used. Checked the real backend over plain HTTP to investigate the reported "recent territory algorithm didn't apply to the real server" issue. `http://localhost:8080/territory` returned `200` and references `assets/Territory-DzrKmJ5J.js`; fetching that chunk from the real server returned `200` and contains `lower-east-visible-bridge-junction`, `global-owner-territory-cache-v821-payload-aware-territory`, and the minified per-group array guard marker. `/territory`, `index-SuSO5bj1.js`, and `Territory-DzrKmJ5J.js` all serve `Cache-Control: no-cache, no-store, max-age=0, must-revalidate`. Conclusion: backend static runtime is not stale and is serving the current algorithm; if the authenticated display still looks unchanged, the next cause is geometry/data gates producing no visual delta or client-side authenticated state, which still requires permitted live screenshot/scorer proof to decide keep/revert.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Hardened the pending `lower-east-visible-bridge-junction` source normalization in `frontend/src/pages/Territory.jsx`: `supportRegionGroups` are now flattened only after each group is confirmed with `Array.isArray(group) ? group : []`, preventing malformed non-array entries from becoming upstream support recovery candidates. Updated the backend wiring smoke guard to enforce this per-group array check along with the existing context and width gates. Verification passed: backend wiring smoke, heatmap world-map smoke, `node --check .tools/score-territory-reference.mjs`, frontend production build, `verify-frontend-runtime-sync`, and marker checks in both backend static locations for `Territory-DzrKmJ5J.js` confirming `lower-east-visible-bridge-junction`, deterministic overlap/east-distance sort, and array guard markers survived minification. Browser/screenshot proof and fresh scorer regeneration remain intentionally not run, so this synced candidate is still pending visual acceptance/revert.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Strengthened the pending `lower-east-visible-bridge-junction` per-candidate context gate in `frontend/src/pages/Territory.jsx`: each upstream support-region recovery candidate must now overlap the generated bridge-junction context in both latitude and longitude before it can emit, via `verticallyOverlapsContext && horizontallyOverlapsContext`. Updated the backend wiring smoke guard to enforce the four-argument `overlapsGeneratedBridgeJunctionContext(westLng, eastLng, southLat, northLat)` call and horizontal context overlap. Verification passed: backend wiring smoke, heatmap world-map smoke, `node --check .tools/score-territory-reference.mjs`, frontend production build, `verify-frontend-runtime-sync`, and marker checks in both backend static locations for `Territory-DCv5GvRM.js` confirming `lower-east-visible-bridge-junction` plus deterministic overlap/east-distance sort survived minification. The explicit context helper names are minified out of the bundle, so the source smoke guard is authoritative. Browser/screenshot proof and fresh scorer regeneration remain intentionally not run, so this synced candidate is still pending visual acceptance/revert.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Further tightened the pending `lower-east-visible-bridge-junction` source/context coupling in `frontend/src/pages/Territory.jsx`: valid generated bridge-junction context bounds are now precomputed from generated support-bridge regions that overlap the lower-east bridge west edge, and each upstream support-region recovery candidate must vertically overlap that generated context before it can emit. Updated the backend wiring smoke guard to enforce `generatedBridgeJunctionContextBounds` plus per-candidate `overlapsGeneratedBridgeJunctionContext(...)`. Verification passed: backend wiring smoke, heatmap world-map smoke, `node --check .tools/score-territory-reference.mjs`, frontend production build, `verify-frontend-runtime-sync`, and marker checks in both backend static locations for `Territory-BCYQcRpw.js` confirming `lower-east-visible-bridge-junction` plus deterministic overlap/east-distance sort survived minification. The explicit context helper names are minified out of the bundle, so the source smoke guard is authoritative. Browser/screenshot proof and fresh scorer regeneration remain intentionally not run, so this synced candidate is still pending visual acceptance/revert.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Tightened the pending `lower-east-visible-bridge-junction` context gate in `frontend/src/pages/Territory.jsx`: generated support-bridge context must now actually overlap the lower-east bridge west edge before any upstream-source recovery rectangle can emit. Added a backend wiring smoke guard for `hasGeneratedBridgeJunctionContext` so unrelated generated bridge regions cannot justify the junction. Verification passed: backend wiring smoke, heatmap world-map smoke, `node --check .tools/score-territory-reference.mjs`, frontend production build, `verify-frontend-runtime-sync`, and marker checks in both backend static locations for `Territory-Dm5XVGA8.js` confirming `lower-east-visible-bridge-junction` plus deterministic overlap/east-distance sort survived minification. The explicit context-gate variable name is minified out of the bundle, so the source smoke guard is authoritative. Browser/screenshot proof and fresh scorer regeneration remain intentionally not run, so this synced candidate is still pending visual acceptance/revert.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Tightened the pending `lower-east-visible-bridge-junction` source path so recovery rectangles are derived only from upstream `supportRegionGroups` and no longer from generated `supportBridgeRegions`; the helper still requires generated support-bridge context to exist before emitting the junction. Updated the backend wiring smoke guard to enforce upstream-source derivation plus generated-bridge context separation. Verification passed: backend wiring smoke, heatmap world-map smoke, `node --check .tools/score-territory-reference.mjs`, frontend production build, `verify-frontend-runtime-sync`, and concise live/static marker check for `Territory-DRJT4_6q.js` showing `lower-east-visible-bridge-junction` and overlap/east-distance sort markers in both backend static locations. Browser/screenshot proof remains intentionally not run, so this synced candidate is still pending visual acceptance/revert.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Added proof-time scorer awareness for the pending `lower-east-visible-bridge-junction` lineage without changing rendered Territory geometry: source-backed underpaint touching that lineage is now classified as already-pending junction work, and remove candidates touching it get a distinct `lower-east-visible-bridge-junction-boundary` risk family instead of being folded into generic bridge/body categories. Added guards in `territoryBackendWiring.smoke.test.js`. Verification passed: backend wiring smoke, heatmap world-map smoke, `node --check .tools/score-territory-reference.mjs`, and scorer regeneration from the existing proof artifact. Baseline metrics stayed unchanged: `iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`; add counts `unsupported=11`, `weak-source=4`, `display-backed-raster-artifact=22`, `source-backed-boundary-underpaint=4`, `actionable-source-backed-gap=0`; remove counts `proof-critical-body-trim=14`, `true-positive-loss=19`, `source-backed-overfill=13`, `display-only-body-overfill=2`, `actionable-source-weak-overfill=0`.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Tightened the synced `lower-east-visible-bridge-junction` candidate diagnostics without changing rendered geometry: generated support/lower-east bridge regions are no longer reported as `rawSourceRegions` for the junction entry, and bridge/lower-east bridge generated tile metadata is exposed through `displayTileRegions`/`displayTileKeys` instead. Added a backend wiring smoke guard so generated bridge tiles remain separate from raw upstream support source evidence. Verification passed: backend wiring smoke, heatmap world-map smoke, `node --check .tools/score-territory-reference.mjs`, frontend production build, `verify-frontend-runtime-sync`, and live/static bundle marker check in `Territory-CKqq8M2C.js`. Browser/screenshot proof and fresh scorer regeneration remain intentionally not run under the current restriction, so this is still a synced candidate pending visual acceptance/revert.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Implemented and tightened a bounded source-derived candidate for the `lower-east-visible-bridge-junction-underpaint` hypothesis in `Territory.jsx`: `activeRouteSupportedLowerEastVisibleBridgeJunctionRegions(...)` creates narrow west-edge recovery rectangles only from existing visible/support-group overlap with the lower-east bridge, with `70m` overlap/availability gates, width capped by `Math.min(170m, bridgeWidth * 0.42, availableWestMeters)`, and deterministic ranking by overlap height then east-edge distance before `slice(0, 4)`. The new lineage is `lower-east-visible-bridge-junction`, guarded in `territoryBackendWiring.smoke.test.js` to prevent screen-coordinate patches, broad bridge fills, or input-order-dependent caps. Verification passed: backend wiring smoke, heatmap world-map smoke, scorer syntax check, frontend production build, `verify-frontend-runtime-sync`, and direct marker check in live/static `Territory-Bb7SzQXA.js`. Browser/screenshot proof and fresh scorer regeneration were intentionally not run under the current restriction, so this candidate is synced but not visually accepted; next permitted step is live filled-land proof plus scorer and revert on proof/score/shape regression.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Added diagnostic-only scorer taxonomy for `source-backed-boundary-underpaint` in `.tools/score-territory-reference.mjs`, guarded by `likelyBoundaryUnderpaint`, at least `24` missing all-current reference pixels, and `sourceReferenceRatio >= 0.45`. Added source-level hypothesis grouping with compact layer lineage and regenerated `.ai-codex/territory-reference-grid-diagnostic.json` from the existing proof artifact; accepted score stayed unchanged (`iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`), but add counts now split to `unsupported=11`, `weak-source=4`, `display-backed-raster-artifact=22`, `source-backed-boundary-underpaint=4`, `actionable-source-backed-gap=0`. Top new candidate is `596,237,12,54` (`deltaIou=+0.004469122426868921`, `missingAll=33`, `sourceReferenceRatio=0.8526`, `displayReferenceRatio=0.8415`) with `sourceHypothesis.family=lower-east-visible-bridge-junction-underpaint` on the west edge of `route-supported-body` / `bodyRegionLineage=lower-east-bridge`. Next source action: test a source-derived lower-east bridge west-edge recovery that connects only already visible/support-group-backed boundary pixels. Added source guards in `territoryBackendWiring.smoke.test.js`; verification passed: backend wiring smoke, heatmap world-map smoke, and `node --check .tools\score-territory-reference.mjs`. No rendering source change was applied because browser/screenshot proof remains restricted.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Root-caused why recent Territory payload/algorithm changes could fail to apply on the real server: `/api/territory/polygons` used one ETag/signature for different response variants (`cells=false`, `initial=true`, `ownFootprint=true`), and the frontend ignored same-server-signature polygon payloads unless active concrete cell count increased. Fixed the cache boundary by adding variant-aware polygon ETags/signatures in `TerritoryController`, tracking `polygonPayloadSignatureRef` in `Territory.jsx`, committing same-server-signature payload changes when the payload hash changes, invalidating derived render cache for changed payloads, and bumping the frontend Territory cache version to `global-owner-territory-cache-v821-payload-aware-territory`. Added regression guards in `TerritoryControllerTests#polygonsEndpointDoesNotCacheCellsFalseAsCanonicalPayload` and `territoryBackendWiring.smoke.test.js`. Verification passed: backend wiring smoke, heatmap world-map smoke, targeted backend variant-cache test, adjacent normal 304 backend test, backend compile, frontend build, frontend runtime sync for bundle `Territory--c4ZFu6V.js`, and backend runtime sync with `http://localhost:8080 -> 200`. Browser display proof was intentionally not run due user restriction.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Tested a source-grid-derived lower-east bridge sub-tile east-edge clip (`eastEdgeLng - 0.66 * tileLngSpan`) to remove the scorer's outer `647,237,6,54` current-only fringe without deleting a full bridge tile/column. Candidate built and synced as `Territory-Bi2OR8Qt.js` but failed live filled-land proof: central active body became too small (`largestComponent.size=6463`, box `113x111@541,189`), and lower-east bridge display shrank to `39x54@608,237`. Reverted the candidate and rebuilt/restored live `Territory-sTdL5asR.js`; runtime sync, live proof, and scorer are back to accepted baseline `proofOk=true`, active paths `24`, coverage `0.90`, visible coverage `0.8291666666666667`, `iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`. Avoid lower-east bridge sub-tile east-edge clipping unless paired with a source-backed coverage recovery.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Extended `.tools/score-territory-reference.mjs` diagnostics so `sourceDiagnosticSupport` now reports `displayTilePixels`, `displayTileReferencePixels`, and corresponding ratios both overall and per layer. Syntax check passed and the real-server scorer artifact was regenerated from the current proof; rendering metrics stayed unchanged at accepted baseline `iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`, coverage `0.90`, visible coverage `0.8291666666666667`. Tile-level scorer evidence confirms no safe bridge trim: lower-east overfill cells have generated-tile support but adjacent weak-source/true-positive cells share the same tile neighborhoods (`622,288` has `displayTileRef=24`; `641,237` has `displayTileRefRatio=0.5467`; `581,273` support-group bridge is full-tile true-positive). No Territory geometry change was applied.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Kept diagnostic-only support-group bridge tile attribution in `frontend/src/pages/Territory.jsx`: `support-group-bridge` route-supported-body entries now expose `displayTileRegions` and `displayTileKeys`, matching lower-east bridge diagnostics. Verification passed: backend wiring smoke, heatmap world-map smoke, syntax checks, frontend build, runtime sync, live filled-land proof, and scorer. New live bundle is `Territory-sTdL5asR.js`; score stayed unchanged at accepted baseline `iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`, coverage `0.90`, visible coverage `0.8291666666666667`. New probe evidence: `577,279,9,9` support-group-bridge overfill has only partial tile-edge display support (`displayTileRatio=0.2963`), while adjacent `581,273,9,9` is fully covered by the same bridge tile (`displayTileRatio=1`) and is a true-positive-loss candidate. Avoid full support-group bridge tile/column trimming; it would remove valid reference coverage.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Added diagnostic-only `riskFamily` classification to `.tools/score-territory-reference.mjs` remove candidates and regenerated the current real-server scorer artifact from `task-images/territory-filled-land-runtime-proof-current.png`. Rendering score stayed unchanged: `iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`. New remove risk-family counts: `support-grid-body-cap=14`, `visible-region-boundary=21`, `support-group-bridge-boundary=4`, `lower-east-bridge-boundary=9`. This explains why the ranked remaining trims should not be chased as broad geometry edits: they map to the already proof-critical cap, rejected/no-op visible shelf/fragment families, or lower-east bridge/connector boundary families whose broad and keyed trims have regressed. No Territory source geometry change was applied.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling or browser tab was used. Verified the recent Territory algorithm is applied to the real server: `verify-frontend-runtime-sync` passed against `http://localhost:8080`, with live/static bundle `Territory-DdvBlVR4.js` containing the required markers (`lower-east-evidence`, `lower-east-connector`, `bodyRegionLineage`, `rawSourceRegions`, `displayTileRegions`, `displayTileKeys`). Regenerated live filled-land proof and scorer from the real server: `proofOk=true`, `activePathCount=24`, coverage `0.90`, visible coverage `0.8291666666666667`, `iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`, `allCurrentIou=0.27441036294757426`. Diagnostics still show no safe add or weak-overfill action: add counts `unsupported=11`, `weak-source=4`, `display-backed-raster-artifact=26`, `actionable-source-backed-gap=0`; remove counts `proof-critical-body-trim=14`, `true-positive-loss=19`, `source-backed-overfill=13`, `display-only-body-overfill=2`, `actionable-source-weak-overfill=0`. No geometry edit was applied because the remaining top trim families match previously rejected proof-critical cap/body or source-backed visible-shelf changes.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling was used. Tested lighter west-fragment top caps after rejected `westFragmentMaxLat=0.90`: `0.96` as `Territory-DC7Rd-id.js` and `0.94` as `Territory-vw2MMCBp.js`. Both passed smoke tests, syntax checks, frontend build, runtime sync, live proof, and scorer, but were exact rendered score no-ops versus accepted `Territory-DdvBlVR4.js` (`iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`, coverage `0.90`). Reverted and rebuilt/reverified live `Territory-DdvBlVR4.js`. Avoid `0.96` and `0.94`; the safe west-fragment cap range is no-op while `0.90` is proof-failing.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling was used. Re-tested the upper-east cap notch on the newer `northCut=0.56` baseline. Candidate `Territory-BBSTMVni.js` passed smoke tests, syntax checks, frontend build, and runtime sync, but failed live filled-land proof again: audited all-layer backend-cell coverage `214/240` (`0.8916666666666667`) and visible coverage `197/240` (`0.8208333333333333`). Reverted and rebuilt/reverified live `Territory-DdvBlVR4.js` with proof PASS and accepted score `iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`. Avoid upper-east cap notching unless paired with source-backed coverage recovery.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling was used. Kept improved lower-east evidence `northCut=0.56` with accepted `eastCut=0.38` in `trimmedLowerEastEvidenceRegion(...)`. Real-server bundle `Territory-DdvBlVR4.js` passes smoke tests, syntax checks, frontend build, runtime sync, live filled-land proof, and scorer: `proofOk=true`, `activePathCount=24`, coverage `0.90`, visible coverage `0.8291666666666667`, `iou=0.6078006500541712`, `currentOnly=2415`, `referenceOnly=481`, `allCurrentIou=0.27441036294757426`. Tested `northCut=0.57` as `Territory-C2o3RSwZ.js`; it passed proof but was an exact score no-op versus `0.56`, so it was reverted to the safer `0.56` because `0.58` is known to fail proof. Current accepted live baseline: `Territory-DdvBlVR4.js`.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling was used. Restored and kept accepted lower-east evidence `northCut=0.54` with `eastCut=0.38` in `trimmedLowerEastEvidenceRegion(...)`. Real-server bundle `Territory-x4-ecbA2.js` passes smoke tests, syntax checks, frontend build, runtime sync, live filled-land proof, and scorer: `proofOk=true`, `activePathCount=24`, coverage `0.90`, visible coverage `0.8291666666666667`, `iou=0.6066216216216216`, `currentOnly=2431`, `referenceOnly=480`, `allCurrentIou=0.2742201330810085`. Rejected stronger `northCut=0.58` as `Territory-CqYWU_o4.js` because live proof failed with central active body too small (`largestComponent.size=6529`, box `113x111@541,189`), lower-east-evidence `17x15@663,264`, connector `10x1@653,278`. Avoid `northCut=0.58`; `0.54` is current accepted baseline.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling was used. Tested adjacent lower-east evidence cutoff `eastCut=0.39` between accepted `0.38` and rejected `0.42`. Candidate `Territory-t7Vle0gy.js` passed smoke tests, syntax checks, frontend build, runtime sync, live filled-land proof, and scorer, but was a rendered score no-op relative to `0.38` (`iou=0.6052312255628961`, `currentOnly=2448`, `referenceOnly=480`). Reverted to documented accepted `eastCut=0.38` and rebuilt/reverified live `Territory-Cb_8M3b2.js` with proof PASS and the same accepted metrics. Avoid `eastCut=0.39`.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling was used. Tested adjacent lower-east evidence cutoff `eastCut=0.36` after accepted `0.38` beat `0.34` and rejected `0.42`. Candidate `Territory-r8TpzF17.js` passed smoke tests, syntax checks, frontend build, runtime sync, live filled-land proof, and scorer, but regressed to the previous score class (`iou=0.6042787944025835`, `currentOnly=2463`, `allCurrentIou=0.273807347834034`). Reverted to `eastCut=0.38` and rebuilt/reverified live `Territory-Cb_8M3b2.js` with proof PASS and accepted score `iou=0.6052312255628961`, `currentOnly=2448`, `referenceOnly=480`. Avoid `eastCut=0.36`; `0.38` remains the best tested lower-east evidence east boundary.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling was used. Kept a lower-east evidence boundary refinement: `trimmedLowerEastEvidenceRegion(...)` now uses `eastCut=0.38` instead of `0.34`. Candidate `Territory-Cb_8M3b2.js` passed smoke tests, syntax checks, frontend build, runtime sync, live filled-land proof, and scorer. Score improved to `iou=0.6052312255628961`; `currentOnly` dropped to `2448`, `referenceOnly` is `480`, `allCurrentIou` improved to `0.2739358458348579`, and `actionable-source-weak-overfill` dropped to `0`. Adjacent `eastCut=0.42` passed proof as `Territory-CMDTj2Ee.js` but scored worse (`iou=0.6046699959508706`, `referenceOnly=489`), so it was reverted. New accepted live baseline: `Territory-Cb_8M3b2.js`; avoid `eastCut=0.42`.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling was used. Tested midpoint lower visible shelf trim `southTrimLongitude=0.70` between accepted `0.68` and rejected `0.74`. Candidate `Territory-Dd0KcZqm.js` passed smoke tests, syntax checks, frontend build, runtime sync, and live filled-land proof, but regressed score to `iou=0.6032301480484522`, worsened `referenceOnly` to `487`, and dropped `allCurrentIou` to `0.2680812910938434`. Reverted to `0.68` and rebuilt/reverified live `Territory-DUNcivVO.js` with proof PASS and accepted score `iou=0.6042787944025835`, `currentOnly=2463`, `referenceOnly=478`. Avoid `southTrimLongitude=0.70`.
- 2026-06-22: Continued locally only; no DeepSeek and no browser display/control tooling. Tested an upper-east cap notch on top of the accepted center-row proof anchor by dropping `box.centerX + 1, box.minY` in the shallow-wide `support-grid-body` cap. Candidate `Territory-IyfxIMzj.js` passed static/build/runtime sync and scored higher (`iou=0.6110126754804416`), but failed live filled-land proof because audited all-layer backend-cell coverage dropped to `214/240` (`0.8916666666666667`). Reverted and restored live `Territory-DUNcivVO.js` with proof PASS and accepted score `iou=0.6042787944025835`. Avoid upper-east cap notching unless coverage is recovered elsewhere.
- 2026-06-22: Continued locally only; no DeepSeek and no browser display/control tooling. Kept a source-grid cap-anchor retune in `activeSupportGridBodyRegions(..., { routeSupportedBody: true })`: the shallow-wide `proofWidthAnchor` moved from `box.minY` to `box.centerY` while preserving the same `box.centerX + 2` east-width stabilizer. Candidate `Territory-DUNcivVO.js` passed backend/heatmap smoke tests, syntax checks, frontend build, runtime sync, and live filled-land proof. Score improved from `iou=0.5963351480547072` to `iou=0.6042787944025835`; `currentOnly` dropped `2562 -> 2463`, `referenceOnly` stayed `478`, and `allCurrentIou` improved to `0.273807347834034`. This is the current accepted live baseline.
- 2026-06-22: Continued locally only; no DeepSeek and no browser display/control tooling. Tested a west-only dominant visible top trim by clipping only the western upper fragment to `westFragmentMaxLat=0.90` while preserving accepted `northBandMinLat=0.66`, `westNotchLng=0.48`, and `eastNotchLng=0.83`. Candidate `Territory-GtUgfpsN.js` passed static/build/runtime sync but failed live filled-land proof: central active body was too small (`140x82`, largest size `6520`) and reference score collapsed to `iou=0.28685035842293904`; reverted. Live server is restored to `Territory-B0W-9Lxn.js` with proof PASS and score `iou=0.5963351480547072`. Avoid west-only top clipping; the upper fragment is a proof-critical stabilizer.
- 2026-06-22: Continued locally only; no DeepSeek and no browser display/control tooling. Tested midpoint dominant visible west-notch expansion by changing `westNotchLng` from accepted `0.48` to `0.50`, keeping `northBandMinLat=0.66` and `eastNotchLng=0.83`. Candidate `Territory-DtQbjXIC.js` passed smoke tests, build, runtime sync, and live proof, but regressed score to `iou=0.5950238221281101`, `currentOnly=2587`, `referenceOnly=473`; reverted. Live server is restored to `Territory-B0W-9Lxn.js` with proof PASS and score `iou=0.5963351480547072`, `currentOnly=2562`, `referenceOnly=478`. Avoid `westNotchLng=0.50`.
- 2026-06-22: Continued locally only; no DeepSeek and no browser display/control tooling. Tested a source-relative dominant visible west-stabilizer expansion by changing `westNotchLng` from `0.48` to `0.54` while preserving `northBandMinLat=0.66` and `eastNotchLng=0.83`. Candidate `Territory-DscC6_yh.js` passed smoke tests, build, runtime sync, and live proof, but regressed score to `iou=0.5903361344537815` by increasing visible current-only (`currentOnly=2647`) despite slightly lowering `referenceOnly=473`; reverted. Live server is restored to `Territory-B0W-9Lxn.js` with proof PASS and score `iou=0.5963351480547072`, `currentOnly=2562`, `referenceOnly=478`. Avoid `westNotchLng=0.54`.
- 2026-06-22: Continued locally only; no DeepSeek and no browser display/control tooling. Tested a source-relative southern visible shelf trim by changing `southTrimLongitude` from `0.68` to `0.74` in `trimmedActiveVisibleShelfRegionGroup(...)`, targeting the scorer's west-side visible shelf overfill. Candidate `Territory-BdtpQxyb.js` passed smoke tests, build, runtime sync, and live proof, but regressed score to `iou=0.5926417850976226`, `referenceOnly=507`, and `allCurrentIou=0.2653158238754531`; reverted. Live server is restored to `Territory-B0W-9Lxn.js` with proof PASS and score `iou=0.5963351480547072`, `currentOnly=2562`, `referenceOnly=478`. Avoid `southTrimLongitude=0.74`.
- 2026-06-22: Continued locally only; DeepSeek remains stopped and no browser display/control tooling was used. Tested the keyed lower-east bridge-row hypothesis by keeping only the westmost generated tile on the initial `best.y` row while preserving the rest of the bridge. Candidate `Territory-DYKNL9TZ.js` passed smoke tests, build, runtime sync, and live filled-land proof, but regressed score to `iou=0.5953813855843247` with `referenceOnly=715`; reverted and rebuilt live server back to `Territory-B0W-9Lxn.js`. Restored proof PASS and score `iou=0.5963351480547072`, `currentOnly=2562`, `referenceOnly=478`. Avoid trimming the initial bridge row to only `best.minX`; it removes useful lower-east connection coverage.
- 2026-06-22: User stopped DeepSeek usage; continue Territory work locally only. Kept diagnostic-only route-supported-body raw source lineage in `frontend/src/pages/Territory.jsx` so each generated body path reports its own raw inputs instead of the merged body input set. Real-server bundle `Territory-BtjGnDAC.js` passes runtime sync, smoke tests, live filled-land proof, and scorer with unchanged accepted rendering: `iou=0.5963351480547072`, `currentOnly=2562`, `referenceOnly=478`, `allCurrentIou=0.2721656976744186`, `activeLandPathCount=24`. Root cause clarified: the two remaining lower-east trim candidates `649,279,9,9` and `640,288,9,9` are display-only overfill (`rawSourceRatio=0`) from `lower-east-connector`/`lower-east-bridge`; no geometry change was kept.
- 2026-06-22: Tested and rejected north-anchoring the lower-east connector within the same source-derived evidence bounds. Candidate `Territory-BImk7Zn3.js` passed proof but regressed score to `iou=0.5834243582741672`, `currentOnly=2355`, `referenceOnly=696`; reverted and reverified `Territory-BtjGnDAC.js` at `iou=0.5963351480547072`, `currentOnly=2562`, `referenceOnly=478`. Continue locally only; do not use DeepSeek unless user re-allows it.
- 2026-06-22: Kept diagnostic-only lower-east bridge tile attribution. Live bundle `Territory-B0W-9Lxn.js` passes proof and unchanged score `iou=0.5963351480547072`. Probe evidence: `649,279,9,9` overlaps bridge tile `-23936:17442`; `640,288,9,9` overlaps `-23936:17442` and `-23937:17442`; nearby `622,288,9,9` overlaps `-23938:17442`. Next attempt should be keyed/tile-specific, not broad bridge row/connector movement.
