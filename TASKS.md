# Hermes Tasks

Use this file as the working queue for AI agents.

**Before choosing any task, read `PRODUCT.md` and ask:**
1. Does this serve The Competitor, The Builder, or The Enthusiast runner persona?
2. Does this move a screen closer to its Product Intent (see `PRODUCT.md` Screen table)?
3. Is this Tier 1 (Daily Coach Value) or Tier 2 (Data Trust) priority? If not, is there a Tier 1/2 gap that should be addressed first?

## Rules
- Keep tasks in priority order, top to bottom.
- Unless explicitly told otherwise, work through `## Active Tasks` first.
- Each checkbox is a separate required deliverable.
- Treat `Files:`, `Context:`, `Done when:`, `Verify:`, `Note:`, and `Blocker:` as hard execution hints.
- After completing a task, add a short `Note:` line, then delete the entire completed task block from `## Active Tasks` and append one short line to `## Daily Log`.
- Keep `## Daily Log` day-scoped. When adding the first entry for a new date, delete older-date entries first.

## Daily Log
- 2026-08-16: Fixed the slow weather page (user: "the weather page is also loading extremely slow"): the first paint was gated behind browser geolocation with an 8s timeout — an unanswered permission prompt held the whole page on its skeleton even though /api/profile/me returns in 3ms. The page now paints immediately from the server-side location (profile → context → forecast chain), and geolocation became a background refinement (3.5s cap) that re-fetches the forecast AND the location-aware heat-adaptation context when device coordinates arrive. Browser-verified live: full content in ~0.9–1.3s. New guardrail `weatherEngineFastPaint.smoke.test.js`; `weatherEditorialRedesign.smoke.test.js` updated to the refinement shape. Full suite 197/198 (remaining failure is the concurrent session's shoes edit).
- 2026-08-16: Fixed the overview quick-nav buttons (user: "fix those buttons" — the 运动员/赛事赛道图/… tiles): the six ops-action tiles and the metric toggle cards moved onto the rework card system — profile-card token surface with 18px radius (was hard-bordered 12px glass), circular coral icon chips (was 8px squares), and a hover lift replacing the border-ring. Guardrail extended. Browser-verified live: tiles radius 18px, icon chips 999px, metric cards 18px. Full suite 196/197 (remaining failure is the concurrent session's shoes edit).
- 2026-08-16: Fixed the slow admin dashboard pages (user: "it takes a long time for every dashboard page to load"): /api/admin/overview and /queues took 1.7s each because queueSummaryBody builds all 82 course-map rows per call, and the seeded catalog made per-row elevation-JSON parsing the dominant cost. List rows now skip elevation/climb payloads (list UI draws route thumbnails only; detail endpoint keeps full fidelity) and a 5s TTL memo collapses the three builds per load into one, invalidated on every asset write. Measured post-restart (DB intact, backup taken): overview 1.73s→0.14s cold / 0.02s warm, queues 1.76s→0.013s, race-course-maps 1.76s→0.013s with payload halved (380→183KB), parallel boot payload 178ms. New backend guardrail test pins the elevation skip + TTL cache (17/17). Backend restarted via careful stop with pre-kill DB backup (hermes_db_v2.mv.db.bak-perf-20260816).
- 2026-08-16: Fixed the messy runner shoes card grid (user: "adjust this grid, it is messy and broken" — the shoe cards on /shoes): the atelier/vault redesign stylesheets still assumed the old flat card structure and forced a 3-track grid (128px/1fr/0.22fr) onto the card, squeezing the whole card-top (image + name + chevron) into a 128px column while metrics/actions took the other tracks. Both redesign files now stack the card rows (card-top carries the art/copy/side grid, metrics and actions below); the atelier guardrail updated to the stacked layout and a new `shoeInventoryCardLayout.smoke.test.js` pins the fix. Browser-verified live on /shoes: card-top spans full width with the name beside the art, metrics below (card 797×342, single-column grid). Full suite 196/197 (remaining failure is the concurrent session's shoes edit).
- 2026-08-15: Completed the localStorage quota fix after a recurrence (user: "still"): the origin's quota was hogged by OTHER cache keys — Runs.jsx stored the full uncapped runs array (routePreview point arrays) under `hermes_runs_v1_*`, and old fat dashboard keys for other accounts. Runs.jsx now caps at 500 and slims cached runs to the 8 fields cards render (route previews lazy-fetch via the existing fallback path), and the dashboard's quota fallback now evicts all sibling `hermes_profile_dashboard_*`/`hermes_runs_v1_*` caches before retrying — structurally guaranteeing room. Browser-verified /profile renders; guardrail extended. Full suite 195/196 (remaining failure is the concurrent session's shoes edit).
- 2026-08-15: Fixed the profile dashboard localStorage QuotaExceededError (user-reported console errors): the cache stored full activity objects (route maps/telemetry), blowing past the 5 MB quota for active accounts. The snapshot now projects each cached run down to the 9 fields the first paint reads (~300 bytes/run, ≤500 runs ≈ 150 KB max), and writeJsonStorage gained a quota fallback that drops the stale fat key and retries once so a slim snapshot replaces it. Browser-verified live on /profile (renders normally post-fix). New guardrail `profileDashboardCacheQuota.smoke.test.js`. Full suite 195/196 (remaining failure is the concurrent session's shoes edit).
- 2026-08-15: Converted the shoe spotlight-card action buttons (打开审核台/替换预览/永久删除) from full-width 14px-radius rectangles to compact circular pills (user: image 1 = buttons to fix, image 2 = pill model): the actions row wraps as flex and every button hugs content at 38px/radius 999px — the monitoring stylesheet's `> button { width:100% }` rule needed an important override. Browser-verified live: 6 buttons at 70–93px wide (was 620px). Guardrail extended. Full suite 194/195 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-34`.
- 2026-08-15: Restructured the admin shoes tab into the Profile-style rework system (user request, matching the DV-31 course-maps treatment): clean hero with four stat chips (pending/live ratio/records/sync%), controls card (inputs + save/export/add actions), workbench grid card (queue rail + stage), and catalog/saved-filters cards; old hero stats block and floating health card folded into the hero; shoes buttons adopt the DV-32 soft-pill system. All data wiring and review flows preserved. Browser-verified live (hero/controls/grid/catalog, 7 uniform buttons, zero legacy SectionCards). New guardrail `adminShoesRework.smoke.test.js`. Full suite 194/195 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-33`.
- 2026-08-15: Redesigned the course-maps action buttons (user: 5 crops): secondary actions are soft filled neutral pills (no hard border, 40px, hover lift), rail refresh compact 36px, archives full-width centered, red gradient exclusive to primary CTAs; added !important overrides because later sweeps (all-pages-liquid-glass / admin-monitoring) repainted some pills with hard borders — live-verified all 10 buttons uniform border 0. Backend restarted after the background process died (DB unaffected). Guardrail extended. Full suite 193/194 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-32`.
- 2026-08-15: Restructured the messy admin course-maps tab into a Profile-style layout (user request): clean hero card, rail + stacked white stage cards (head with primary actions, map comparison, signals, decision dock, consolidated actions, scan timeline); removed decorative rail panels, the map-stage headerband, the duplicated publish header, and the ops band's duplicate action groups — all data wiring and actions preserved. Browser-verified live (hero/rail/5 stage cards, zero legacy panels); typecheck clean. Four old-structure guardrails rewritten (two superseded removed, two new added). Full suite 193/194 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-31`.
- 2026-08-15: Added the reference admin-portal charts row to the overview (user clarified the images are external admin-portal references and asked to copy grids + graphs): 用户增长 cumulative line (DV-09 fetch reuse), 每日审计事件 per-day bars (paged audit API bucketed by createdAt, last 14 days), and a 跑鞋图片状态 doughnut (verified/pending/missing from queues + shoe totals) — Chart.js Bar/Doughnut registered, both locales, loading/error/empty states. Browser-verified live: 3 chart cards render; user line + donut draw live data; audit bars show the empty state correctly since the fresh dev DB has zero audit events. New guardrail `adminOverviewCharts.smoke.test.js`. Full suite 194/195 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-30`.
- 2026-08-15: Added a queue-health stat card grid to the admin overview (user: "add grids like these in image"): four white rounded cards (auto-fit 170px grid) for 待发布赛道图/缺失赛道图/待审核跑鞋图片/缺失跑鞋图片 with tone dots (coral when non-zero), big values, and uppercase labels, using the profile-card tokens for theme flips. Browser-verified live: 4 cards at 250×104, values 0/0/0/3, attention dot on 缺失跑鞋图片. New guardrail `adminOverviewQueueCardGrid.smoke.test.js`. Full suite 193/194 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-29`.
- 2026-08-15: Equalized the arrow pager circles optically (user: "make sure two circles are in same size"): both measured 36×36 already; the red next button's drop shadow made it read larger, so the shadow was removed — both arrows now render as identical 36×36 discs (neutral ‹ / red ›). Browser-verified live on /dashboard/users. Full suite 192/193 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-28`.
- 2026-08-15: Paginated the admin accounts table to 5 rows with a new arrow pager (user request): `/api/admin/users` now gets `size=5` (new `USERS_TABLE_PAGE_SIZE`), and the shared `Pagination` became circular arrow buttons — neutral ‹, 第 X/Y 页 indicator, red-gradient › next — applied consistently across all five admin tables (only the users table changed size). Browser-verified live end-to-end with 7 dev users: 5 rows on page 1, › click loads page 2 with 2 rows; three throwaway `pager-test-*@hermes.local` accounts created on the dev DB to exercise paging. New guardrail `adminUsersTablePagination.smoke.test.js`. Full suite 192/193 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-27`.
- 2026-08-15: Ported the login redesign to /signup (user: "redesign signup page based on recent change in login"): corner logo block (HermesMarkSvg + title, shared absolute CSS) in both render branches, social block hidden, legal footer moved inside the card centered under the signin row; carousel trims/centering already shared via the command-entry scope. Browser-verified live on /signup: logo at (106,108), social/legal/slides all matching login. Guardrail extended to pin signup parity. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-26`.
- 2026-08-15: Moved the login card's 条款/隐私/支持 legal row under the 还没有账号？立即注册 row (user request, clarified as a move not an add): markup reordered so the footer renders inside the card right after the signup link, with a centered `--inline` variant (10px gap); the form side's bottom edge is now clear. Browser-verified live: legal row inside the card at 821–835, signup ends 809. Guardrail extended (markup order + inline variant). Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-25`.
- 2026-08-15: Added the website mark left of the HERMES title and pinned the wordmark block (logo + title + pulse) to the login brand panel's top-left corner (user request): `HermesMarkSvg` renders in a centered row before the h1; the wrap is absolutely positioned at 34/44px, superseding the DV-17 wordmark-column alignment. Carousel placement/biases/trims unchanged. Browser-verified live: logo 30×30 at (106,108), title beside it, slides still centered. Guardrail updated (corner pin + logo row + markup). Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-24`.
- 2026-08-15: Added the per-slide kicker pill to the login flow-slide hide list (user: "still there", login only): the kicker was the last chip-shaped element rendering on every slide. Slides now show only the hero headline + paragraph; the wordmark's pulse label stays. Browser-verified live: kicker/details/stats all display:none. Guardrail updated to pin all three hidden slide parts. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-23`.
- 2026-08-15: Removed the social login block from the login card (user: "remove this part" pointing at the disabled Strava/Google buttons + status notes): CSS-scoped hide on the liquid-glass login page; the buttons, OAuth handlers, and signup surface stay intact. Browser-verified live: .auth-flow-social display:none, submit/signup/legal all visible. Guardrail extended (login social block hidden + flow-slide headline rule both pinned). Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-22`.
- 2026-08-15: Corrected the flow-slide trim direction (user: "you remove the part that i am asking to reserve"): the headline block (kicker + hero + paragraph) is kept and the details/stats chip rows are hidden — the inverse of DV-20. Browser-verified live: details/stats display:none, kicker/hero/text visible at the wordmark's left edge (x=173). Guardrail updated to pin the corrected hide rule. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-21`.
- 2026-08-15: Trimmed the login flow slides to the visual chip block only (user: "just keep image part, remove other parts"): kicker, hero headline, and paragraph are hidden on the liquid-glass command-entry slides (CSS-scoped; data/component unchanged, copy returns if the rule is dropped); the details + stats cards keep rotating, 460px wide at the wordmark's left edge. Browser-verified live: kicker/hero/text display:none, chips aligned at x=173. Guardrail extended to pin the hide rule. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-20`.
- 2026-08-15: Nudged the login brand column content right two dot units (user: "move right two dot"): a 96px padding-left bias on the horizontally-centered brand inner shifts the wordmark + slide cluster 48px right, stacking on DV-18's upward bias; left-edge alignment preserved (whole group moves as one). Browser-verified live: left edge 125→173, top unchanged. Guardrail updated to pin both biases. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-19`.
- 2026-08-15: Nudged the entire login brand column content upward (user: "move up entire thing 3 dot points up"): added a 144px padding-bottom bias to the vertically-centered brand inner, lifting the wordmark + slide cluster 72px while keeping the centering behavior. Browser-verified live at 1280×1080: wordmark 282→210, slide 432→360, form card unchanged. Guardrail updated to pin the bias. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-18`.
- 2026-08-15: Aligned the login HERMES wordmark with the flow slide info (user request): the wordmark wrap now uses the same 460px centered column as the slide viewport with content anchored left, so title, pulse label, and slide copy share one left edge (was a narrow centered box, 150px offset from the slide edge at 1920). Browser-verified live at 1920×1080 (wordmark/pulse/viewport all at x=295) and 1280×1080 (all at x=125). Guardrail extended to pin the wordmark column alignment. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-17`.
- 2026-08-15: Centered the login flow slides at wide windows (user at 1920×1080: "flow slides are too left, place it central"): the capped 660px brand inner packed at the panel's left under the command-entry base, leaving the 460px slide block left of the panel center (centerline 408 vs 501). The brand now centers its inner horizontally — at 1920×1080 the slide block sits at 295→755 (centerline 525 = panel content center, matching the wordmark); 1280×1080 unchanged. Guardrail extended to pin the horizontal centering. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-16`.
- 2026-08-15: Fixed the login page at large windows (user: verify at 1280×1080+, "it doesn't work"): the command-entry base stretches `.auth-flow-brand-inner` and packs its children at the top, leaving a ~540px dead zone under the carousel on tall windows while the form column stays centered. The brand inner now centers the wordmark+slide group vertically. Browser-verified live at 1280×1080: content group 282→830 exactly centered in the 64→1048 panel, matching the form card band; also re-verified centered at 1920×1080. Guardrail extended to pin the vertical centering. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-15`.
- 2026-08-15: Followed the user's placement drawing for the login carousel: the slide block is now a fixed 460px reading column horizontally centered in the brand panel on the wordmark's centerline (125→585 in the 62→648 panel, 63px margins each side), replacing DV-13's left-anchored cap; rows inside stay left-aligned. Guardrail updated to pin the centered 460px viewport. Browser-verified live on localhost:8080 `/login` post build+sync. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-14`.
- 2026-08-15: Refined the login carousel column after "still too outward" (DV-12 follow-up): the panel-width stretch let the details/stats chips run to the panel's far-right edge (648px), 214px past the paragraph's 44ch column and only 53px from the form card. The slide viewport now caps at a 460px reading column, left-anchored at the panel padding — hero/text/chips form one coherent column ending near 522px with a ~180px gap to the form. Guardrail extended to pin the viewport cap. Browser-verified live on localhost:8080 `/login` post build+sync. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-13`.
- 2026-08-15: Completed the login carousel placement fix after the user identified the element directly (`auth-flow-slide`, still "placed too outward" after DV-11): the command-entry base centers the `.auth-flow-copy` column's children as well, so the slide viewport shrink-wrapped to ~373px and floated centered in the 586px brand panel with dead margins. The liquid-glass skin now stretches the copy column — the whole slide block spans the panel content width flush at its edges. Extended guardrail `loginSlideChipsAlignment.smoke.test.js` to pin the copy-column stretch. Browser-verified live on localhost:8080 `/login` post build+sync (copy/viewport/slide/details 62→648 panel-flush). Note: two earlier "didnt apply" reports were partly stale tabs holding the pre-fix bundle in memory; reloaded the user's tabs. Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-12`.
- 2026-08-15: Fixed the login carousel chip rows floating outward (user report with crop + full-page screenshots of /login): the auth command-entry base rule centers slide children (`align-items: center`), so the slide-details/stats chip rows shrink-wrapped and drifted off the left-aligned text stack at wider viewports. The liquid-glass skin now stretches slide children, spans the details grid full slide width, and pins the paragraph margin — every layer flush on the slide's left edge. Also fixed en route: the admin course-map decision dock collapsed to one column at ≤1360px, leaving the compact CTA pill alone on a far-edge row; the dock is now a centered flex row (verdict left, pill right). New guardrails `loginSlideChipsAlignment.smoke.test.js` and extended `adminInlineButtonsCircular.smoke.test.js`. Browser-verified live on localhost:8080 (/login slide alignment 169→542 flush; dock 87px single row). Full suite 191/192 (remaining failure is the concurrent session's shoes edit). Design logs `DV-2026-08-15-11`, `DV-2026-08-15-10`.
- 2026-08-15: Converted the admin console's stretched bar buttons to compact circular pills (user request with 6 screenshots + pill reference): every `.admin-command-page .btn-inline-md` action (运行提取管线 / AI 扫描上传 / 开始 Strava 同步 / 添加跑鞋 / 添加到目录 / 登出) now hugs its content with `border-radius: 999px` and centered content; the course-map workbench action rows switched from a stretching `minmax(150px,1fr)` grid to wrapping flex; the stage-header row centers its pills instead of stretching them to 91px; the decision-dock primary CTA shrank from a 649×52 full-dock bar to a compact right-aligned 44px pill, keeping the red gradient. New guardrail `adminInlineButtonsCircular.smoke.test.js`. Browser-verified live on localhost:8080 post build+sync (measured pill geometry on jobs/shoes/settings/course-maps tabs). Full suite 190/191 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-10`.
- 2026-08-15: Fixed the extremely slow admin dashboard (user report): root cause was `GET /api/admin/race-course-maps` shipping full-fidelity `PreviewSnapshot`s for every row (materialized base64 preview PNGs up to 8.5MB + ~700-point routes per preview, ×2 previews ×82 maps = 147MB), plus `/api/admin/overview` and `/api/admin/queues` embedding 8 full rows each (14.3MB apiece) — ~176MB across the boot `Promise.all`. Added a list-mode snapshot builder in `RaceCourseMapService` (no materialized `previewImageUrl`, route points downsampled to 32; the per-race detail endpoint keeps full fidelity for the workbench). Measured: overview 4.4s/14.3MB → 0.16s/3KB; queues 3.0s/14.3MB → 0.08s/1.5KB; race-course-maps 3.5s/147MB → 0.08s/191KB; browser dashboard boot 10-25s → ~0.7s. New backend guardrail test pins the coarse list rows (16/16 suite pass). Incident note: the backend restart during this fix reset the H2 file DB (pre-restart `hermes_db_v2.mv.db` unrecoverable after the hard kill; fresh DB auto-reseeded; `hermes_db_v2.mv.db.bak-rebuild-20260814` from Aug 14 11:51 is available to restore). Resolution: user chose to keep the fresh auto-seeded DB and accept the loss of accumulated dev data; old backup files remain on disk untouched.
- 2026-08-15: Applied the Profile-page navigation design to the admin console sidebar (user request): nav items became single-label pills (icon + label + tiny index, 42px, radius 15px) with the runner radial-gradient active pill and coral edge bar, the brand block moved to the profile brand-copy pattern, and the footer logout became the gradient CTA pill; new `--admin-nav-*` tokens mirror the runner set with midnight/high-contrast redefinitions. New guardrail `adminSidebarProfileNav.smoke.test.js`. Live verified light theme on localhost:8080 `/dashboard` post build+sync. Full suite 188/189 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-08`.
- 2026-08-15: Fixed the giant admin sidebar icons (user report): `AppIcon` SVGs carry only a viewBox, so in light theme nothing sized them — the 300px intrinsic default was clamped by the container to ~96-111px inside 42-52px nav buttons. Added the light-theme counterpart (`width/height: 1.15rem`) to the midnight rule that already lives in `dark-mode-cohesion.css`, in `admin-kinetic-editorial.css`. New guardrail `adminSidebarIconSizing.smoke.test.js` pins both halves of the pair. Live verified post build+sync: all 8 sidebar icons now 18x18px. Full suite 186/187 (remaining failure is the concurrent session's shoes edit). Design log `DV-2026-08-15-07`.
- 2026-08-15: Fixed the broken admin dashboard overview (user report, reproduced live with an admin session): removed the sidebar wordmark printing "HERMES" twice beside HermesLogo, dropped the sidebar footer CTA duplicating the course-maps nav item, removed the `ops-metric-kicker` line repeating the metric label translation key (every KPI label rendered twice), and made queue counts shape-aware via `toCount` so 待发布赛道图 shows 8 instead of NaN (`/api/admin/queues` sends `pendingRaceCourseMaps` as an array; old `Number(array)` = NaN). New guardrail `dashboardOverviewDedup.smoke.test.js`. All 26 dashboard suites pass; full suite 186/187 (remaining failure is the concurrent session's shoes edit). Live `/dashboard` DOM verified post build+sync. Design log `DV-2026-08-15-06`.
- 2026-08-15: Whitened the Settings connected-services grid: the runner-shell `[class*="-card"]` liquid-glass sweep was repainting the Strava and Garmin `.st-service-card`s with cream `--hermes-glass-paper` plus blur. Added a light-theme-only `#ffffff` guard (background-image and backdrop-filter cleared) to `grid-cards-white.css`, which `index.css` imports last; midnight/high-contrast surfaces untouched. New guardrail test `settingsServiceCardsWhite.smoke.test.js` pins the white guard, its light-only scoping, and the last-position import. Design log `DV-2026-08-15-05`.
- 2026-08-15: Turned the Settings wellness hub grid (`st-wellness-row` rows on /settings 健康数据同步) pure white: replaced the `rgba(0,0,0,0.02)` gray tint with `#ffffff` and added a theme-aware `var(--line-soft)` border so the rows stay readable on the white card; the midnight/high-contrast row surface is untouched. Extended `settingsWellnessHub.smoke.test.js` to pin the white background and reject the old tint, and regenerated the legacy style bundle. Frontend build + static sync verified and localhost:8080 returns 200 serving the bundle with the white rule; contract suite 184/186 (the 2 remaining failures are the concurrent session's uncommitted runs/shoes edits). Design log `DV-2026-08-15-04`.
- 2026-08-15: Turned the Analysis VO2max trend hero (`analysis-profile-primary` on /analysis) into a plain white card. It was transparent by design, so the cream analysis canvas showed through as beige; changed the base rule, the doubled-class specificity clamp, and both liquid-glass overrides to `#ffffff !important` (midnight surfaces untouched) and updated the accent guardrail test to pin white at all three specificity tiers. Live bundle verified white; live in-browser /analysis check blocked by the local login flow not responding (concurrent session in-flight). Design log `DV-2026-08-15-03`. Note: contract-suite failures now fluctuate run-to-run (runsHeroOverlayContrast, shoesGridVisibility, runDetailProfileCockpit, runnerShellWorkoutButtonParity) because a concurrent session is editing frontend sources mid-test; analysis tests pass.
- 2026-08-15: Completed the white-card fix the user flagged as still beige: the runner-shell liquid-glass sweep (`[class*="-card"]` matcher in `all-pages-liquid-glass.css`) was repainting the Profile bottom-grid cards (predictions/stamina/streak) and weekly digest with the cream `--hermes-glass-paper` over the white token. Extended the white-surface `:is()` override to cover them, updated the glass-surface guardrail test to pin the white design, and added a regression assertion for the bottom grid. Live browser computed-style proof on localhost:8080: all six Profile card groups now `rgb(255,255,255)`. Contract suite 184/185 (remaining failure is the concurrent session's uncommitted `runs.css` whitening breaking `runsHeroOverlayContrast`). Design log `DV-2026-08-15-02`.
- 2026-08-15: Turned the Profile dashboard grid-card backgrounds pure white (`--hd-bg-card` from `rgba(255,255,255,0.92)` to `#ffffff`, light mode only) after the 92%-opacity white read beige over the cream page. Design log `DV-2026-08-15-01`; dark theme and Run Detail tokens untouched. Frontend build, static sync, and full 185-test source-contract suite PASS.
- 2026-08-15: Security-hardening continuation round. Audited all 20 checklist items against the live code (most already enforced in earlier rounds: PBKDF2 passwords, auth/role filter chain, ownership-scoped record access, rejectUnexpectedFields mass-assignment guard, encrypted Strava/Garmin secrets, rate limiters, reCAPTCHA, parameterized SQL, upload allowlists, trimmed JSON responses, security headers, HTTPS forcing, CodeQL/Trivy/Dependabot; git-history secret probe clean). Closed the remaining gaps: `StravaWebhookController` now fails closed on blank/development-default verify tokens in production (dead duplicate deauth block removed, env detection aligned with `hermes.environment`), and `SecurityHeadersFilter` CSP `script-src` no longer allows `'unsafe-inline'` (built SPA verified to have zero inline scripts). Added `docs/repo-rules/security.md` checklist enforcement map linked from the repo-rules index. Backend compile plus targeted webhook/security-headers test suites PASS (20/20).


## Active Tasks
- [x] Improve Analysis page
  Files: `frontend/src/pages/Analysis.jsx`
  Problem: frontend-design
  Owner: frontend-agent
  Context: Pages index resolves Analysis to an existing frontend page (Quality Score: 1400).
  Done when: the website-audit fallback candidate is investigated and the bounded surface issue is resolved with targeted verification.
  Verify: cd frontend && npm run lint && npm run build
  Surface: Analysis
  Website Audit: fallback-selected
  Website Audit Summary: Controller reported no promotable work; website audit selected Analysis as the bounded fallback candidate (frontend/src/pages/Analysis.jsx).
  Result: Integrated the existing heat-adaptation context into Analysis with six live engine metrics, historical per-run correction coverage, an exposure-day track, bilingual interpretation, and responsive/dark presentation without changing VDOT, zone, or prediction methodology.
  Verified: focused heat-adaptation, VDOT, and Weather smoke tests; frontend lint; production Vite build/static sync; authenticated desktop and mobile browser checks with no horizontal overflow or console errors.

## Tech Debt Tasks

### Territory Map Debt
### Backend Debt
